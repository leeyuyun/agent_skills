# Search Plan Template

## 1. Review Contract

1. Research question:
2. Scope in:
3. Scope out:
4. Primary target:
- Course assignment / Thesis / Journal-Conference / Internal briefing
5. Binding constraints:
- Department handbook:
- Submission guideline:
- Required template:
6. Audience:
7. Date window (`from`, `to`, `as_of`):
8. Review depth (rapid, structured, systematic-lite):
9. Citation target (default `30-40`):
10. Selected methodology:
11. Draft format (`.md` / `.docx` / `.tex`):
12. Final output format (usually `.pdf` for formal deliverables):
13. Citation style decision (priority: guideline/school > discipline > region):
14. Bibliography source file (`.bib` preferred, `.ris` acceptable):
15. Report slug:
16. Output directory:

## 2. Source Portfolio Design

Choose by role:
1. Broad discovery source:
2. Domain-specific source:
3. Preprint source:
4. Optional policy/standards source:
5. Optional citation graph source:

Minimum recommendation:
1. At least one broad scholarly index (for example Google Scholar, OpenAlex, Crossref).
2. At least one domain index (for example PubMed, IEEE Xplore, ACM DL, SSRN).
3. At least one preprint source (for example arXiv, bioRxiv, medRxiv).
4. Optional policy/standards source when topic needs governance evidence.

## 3. Query Strategy

1. Core concept terms:
2. Synonyms and variants:
3. Context terms:
4. Exclusion terms:
5. Boolean query:
6. Semantic query text:
7. Citation expansion seeds:

Boolean skeleton:

```text
("TERM_A" OR "TERM_B")
AND
("CONTEXT_A" OR "CONTEXT_B")
NOT
("EXCLUDE_A" OR "EXCLUDE_B")
```

## 4. Query Log

For each query run, capture:
1. Source:
2. Full query:
3. Filters:
4. Execution date:
5. Returned count:
6. Included count:
7. Notes:

## 5. Search QA Checklist

1. PRESS-style review completed:
2. PRISMA-S fields logged:
3. Query reproducibility verified:
4. Duplicate handling rule defined:
5. Citation data captured into bibliography source file (`.bib`/`.ris`) during screening:

## 6. Screening Criteria

Inclusion criteria:
1.
2.
3.

Exclusion criteria:
1.
2.
3.

## 7. Citation and Bibliography Notes

1. Reference manager/tool used:
2. Export format (`.bib`/`.ris`):
3. Missing metadata handling rule (DOI, venue, date, URL):
4. Final style rendering method (citeproc/journal template/manual exceptions only):
